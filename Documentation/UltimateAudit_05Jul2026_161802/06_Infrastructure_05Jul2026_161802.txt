================================================================================================
PLANTPROCESS IQ INFRASTRUCTURE AUDIT - DEPLOYMENT / DOCKER / CADDY / CI-CD
================================================================================================

Product Name          : PlantProcess IQ
Generated At          : 2026-07-05 16:20:05
Repository Root       : C:\Workspace\PlantProcess-IQ
Output Folder         : C:\Workspace\PlantProcess-IQ\Documentation\UltimateAudit_05Jul2026_161802
Included Files        : 21
Total Lines           : 2887
Total Size            : 0.139 MB
Mask Secrets          : False
Compute Hashes        : True
Audit Signals         : True
Max File Size         : 8 MB
PowerShell Version    : 5.1.26100.7920
Machine Name          : WAP174432
User Name             : ELKA01

------------------------------------------------------------------------------------------------
CATEGORY SUMMARY
------------------------------------------------------------------------------------------------

Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins) Files:    21 | Lines:     2887 | Size:    0.139 MB

------------------------------------------------------------------------------------------------
CLASSIFICATION SUMMARY
------------------------------------------------------------------------------------------------

Caddyfile                                  Files:     2 | Lines:      140
Dockerfile                                 Files:     3 | Lines:       59
GitHub Actions Workflow                    Files:     1 | Lines:       18
Jenkins Pipeline                           Files:    15 | Lines:     2670

------------------------------------------------------------------------------------------------
EXTENSION / SPECIAL FILE SUMMARY
------------------------------------------------------------------------------------------------

.yml                 Files:     1 | Lines:       18 | Size:     0.39 KB
[NO_EXTENSION]       Files:    20 | Lines:     2869 | Size:    141.8 KB

------------------------------------------------------------------------------------------------
TABLE OF CONTENTS
------------------------------------------------------------------------------------------------

    1. .github\workflows\ppiq-security-and-hygiene.yml | GitHub Actions Workflow | 18 lines | 0.39 KB
    2. deploy\.ppiq-backups\demo-realism-and-ci-gate-20260622-192328\Jenkinsfile | Jenkins Pipeline | 159 lines | 7.75 KB
    3. deploy\.ppiq-backups\demo-realism-and-ci-gate-20260622-194449\Jenkinsfile | Jenkins Pipeline | 175 lines | 8.91 KB
    4. deploy\.ppiq-backups\deploy-batch2-20260625-140644\Jenkinsfile | Jenkins Pipeline | 176 lines | 9.09 KB
    5. deploy\.ppiq-backups\jenkins-siblings-20260625-144742\Jenkinsfile | Jenkins Pipeline | 184 lines | 9.65 KB
    6. deploy\.ppiq-backups\line101-20260625-153248\Jenkinsfile | Jenkins Pipeline | 193 lines | 10.05 KB
    7. deploy\.ppiq-backups\reproducible-env-20260622-142358\Jenkinsfile | Jenkins Pipeline | 163 lines | 7.9 KB
    8. deploy\.ppiq-backups\reproducible-env-20260622-143803\Jenkinsfile | Jenkins Pipeline | 159 lines | 7.76 KB
    9. deploy\.ppiq-backups\reproducible-env-20260622-144139\Jenkinsfile | Jenkins Pipeline | 159 lines | 7.76 KB
   10. deploy\.ppiq-backups\reproducible-env-20260622-145837\Jenkinsfile | Jenkins Pipeline | 159 lines | 7.76 KB
   11. deploy\.ppiq-backups\stage4-heredoc-fix-20260625-152547\Jenkinsfile | Jenkins Pipeline | 198 lines | 10.13 KB
   12. deploy\.ppiq-backups\stage4-quote-20260625-151530\Jenkinsfile | Jenkins Pipeline | 191 lines | 9.86 KB
   13. deploy\.ppiq-backups\stage4-shfix-20260625-152742\Jenkinsfile | Jenkins Pipeline | 193 lines | 10.05 KB
   14. deploy\.ppiq-backups\stage9-extract-20260626-094753\Jenkinsfile | Jenkins Pipeline | 193 lines | 10.03 KB
   15. deploy\.ppiq-backups\stage9-user-20260626-095520\Jenkinsfile | Jenkins Pipeline | 184 lines | 9.52 KB
   16. deploy\.ppiq-backups\v1-04-caddydefaults-20260628-163957\Caddyfile | Caddyfile | 70 lines | 2.21 KB
   17. deploy\.ppiq-backups\web-dockerfile-smoke2-20260626-131101\Dockerfile | Dockerfile | 24 lines | 0.53 KB
   18. deploy\caddy\Caddyfile | Caddyfile | 70 lines | 2.23 KB
   19. deploy\edge-agent\Dockerfile | Dockerfile | 12 lines | 0.39 KB
   20. Jenkinsfile | Jenkins Pipeline | 184 lines | 9.53 KB
   21. Website\PlantProcess.Website\Dockerfile | Dockerfile | 23 lines | 0.67 KB

------------------------------------------------------------------------------------------------
INCLUDED FILE TREE
------------------------------------------------------------------------------------------------

[.github]
    [workflows]
        ppiq-security-and-hygiene.yml
[deploy]
    [.ppiq-backups]
        [demo-realism-and-ci-gate-20260622-192328]
            Jenkinsfile
        [demo-realism-and-ci-gate-20260622-194449]
            Jenkinsfile
        [deploy-batch2-20260625-140644]
            Jenkinsfile
        [jenkins-siblings-20260625-144742]
            Jenkinsfile
        [line101-20260625-153248]
            Jenkinsfile
        [reproducible-env-20260622-142358]
            Jenkinsfile
        [reproducible-env-20260622-143803]
            Jenkinsfile
        [reproducible-env-20260622-144139]
            Jenkinsfile
        [reproducible-env-20260622-145837]
            Jenkinsfile
        [stage4-heredoc-fix-20260625-152547]
            Jenkinsfile
        [stage4-quote-20260625-151530]
            Jenkinsfile
        [stage4-shfix-20260625-152742]
            Jenkinsfile
        [stage9-extract-20260626-094753]
            Jenkinsfile
        [stage9-user-20260626-095520]
            Jenkinsfile
        [v1-04-caddydefaults-20260628-163957]
            Caddyfile
        [web-dockerfile-smoke2-20260626-131101]
            Dockerfile
    [caddy]
        Caddyfile
    [edge-agent]
        Dockerfile
Jenkinsfile
[Website]
    [PlantProcess.Website]
        Dockerfile

================================================================================================
FULL FILE CONTENT
================================================================================================

[FILE_START]
[METADATA: Path='.github\workflows\ppiq-security-and-hygiene.yml', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='GitHub Actions Workflow', Lines=18, SizeKB=0.39, SHA256='5D3A7C9ACC5C78F479D7E9766E36C2B91809F01EDF7CD2FBD1743846DFAB10E4', Modified='2026-06-06 08:32:44']

name: ppiq-security-and-hygiene

on:
  push:
  pull_request:

jobs:
  hygiene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: UTF-8 no BOM gate
        shell: pwsh
        run: ./tools/phase1-phase2/Test-Utf8NoBom.ps1 -ProjectRoot .
      - name: Secret scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\demo-realism-and-ci-gate-20260622-192328\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=159, SizeKB=7.75, SHA256='A55804E8A4D06855236CA7543B2B9AE26C4DF6CD20F6625B7A73C7147FB22E55', Modified='2026-06-22 14:01:34']

// =====================================================================================
// PlantProcess IQ — canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> npm run e2e (BLOCK)
//   -> build + recreate canonical stack (health gate + rollback)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file:
//   do NOT add catchError(buildResult:'SUCCESS') and do NOT add --list to any suite.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          test -f "${ENV_FILE}" || { echo "FATAL: ${ENV_FILE} missing on host. Seed it from env/profiles/*.example."; exit 1; }
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 1: main app DB migrated (structure) + seeded (latest data) — model-first.
    stage('3. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # EF-first is owned by the canonical migrate path (migrate-and-seed.sh --app-only):
          # it generates an idempotent script FROM the EF model and applies it BEFORE the
          # numbered SQL. The duplicated inline EF update call was removed (V1-01).
          # 3b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 3c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 2: demo DBs migrated + seeded — only when this host runs them.
    stage('4. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 3-5: every suite blocking; a red suite stops the deploy.
    stage('5. Backend tests — BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          export PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}"
          export ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}"
          export PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}"
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          dotnet test Backend --nologo
        '''
      }
    }

    stage('6. Frontend unit tests — BLOCKING') {
      steps { dir("${FRONTEND_DIR}") { sh 'set -euo pipefail; npm ci; npm run test' } }
    }

    stage('7. Frontend e2e — BLOCKING (ephemeral CI stack)') {
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults — Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED — deploy stages did NOT run. Fix the failing suite; nothing was shipped.' }
    success { echo "PIPELINE GREEN — build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\demo-realism-and-ci-gate-20260622-194449\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=175, SizeKB=8.91, SHA256='FEBD91CD0D5069C3C248F34E9537F05EDCF4D180C3B5187C9A9267D31370162A', Modified='2026-06-22 18:23:28']

// =====================================================================================
// PlantProcess IQ - canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> e2e (BLOCK)
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> build + recreate canonical stack (health gate + roll back on failure)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// Why tests run FIRST: a deploy must be unreachable while any suite is red. Every test
// stage is textually ordered ahead of every DB-migrate, seed and stack-recreate stage,
// so a red suite makes the ship stages unreachable (CiPipelineTruthGateTests /
// DeployRedPathProofTests parse this file to enforce that).
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file: never let a failing suite
// resolve to a green build result, and never enumerate suites instead of executing them.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          test -f "${ENV_FILE}" || { echo "FATAL: ${ENV_FILE} missing on host. Seed it from env/profiles/*.example."; exit 1; }
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 1-3: every suite blocking and ordered BEFORE any migrate/seed/deploy,
    // so a red suite makes the ship stages below unreachable. Each suite owns an ephemeral
    // database/stack and does not depend on the production app DB migrated later.
    stage('3. Backend tests - BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          export PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}"
          export ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}"
          export PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_RUNTIME_CONNECTION_STRING:-$PPIQ_TEST_CONNECTION_STRING}"
          export PPIQ_RLS_TEST_CONNECTION_STRING="${PPIQ_TEST_RUNTIME_CONNECTION_STRING:-$PPIQ_TEST_CONNECTION_STRING}"
          export PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_RUNTIME_CONNECTION_STRING:-$PPIQ_TEST_CONNECTION_STRING}"
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          dotnet test Backend --nologo
        '''
      }
    }

    stage('4. Frontend unit tests - BLOCKING') {
      steps { dir("${FRONTEND_DIR}") { sh 'set -euo pipefail; npm ci; npm run test' } }
    }

    stage('5. Frontend e2e - BLOCKING (ephemeral CI stack)') {
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 4: main app DB migrated (structure) + seeded (latest data) - model-first.
    // Reached only after every suite above is green.
    stage('6. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # 6a. EF migrations FIRST - create the model-derived schema (incl. audit_log_entries)
          #     so the numbered SQL decoration scripts have their dependency tables.
          dotnet ef database update \
            --project "${INFRA_PROJ}" --startup-project "${API_PROJ}" --no-build || \
          dotnet ef database update \
            --project "${INFRA_PROJ}" --startup-project "${API_PROJ}"

          # 6b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 6c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 5: demo DBs migrated + seeded - only when this host runs them.
    stage('7. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    // deploy-canonical.sh performs the health gate and rolls back to the prior image on
    // failure (rollback), so a bad image never stays live.
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults - Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED - deploy/migrate/seed stages did NOT run. Fix the failing suite; nothing was shipped and no rollback was needed.' }
    success { echo "PIPELINE GREEN - build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\deploy-batch2-20260625-140644\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=176, SizeKB=9.09, SHA256='43F26915E272AD98454909E32114B64E91265F1881F9727DC63C81352D46500B', Modified='2026-06-25 14:06:45']

// =====================================================================================
// PlantProcess IQ - canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> e2e (BLOCK)
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> build + recreate canonical stack (health gate + roll back on failure)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// Why tests run FIRST: a deploy must be unreachable while any suite is red. Every test
// stage is textually ordered ahead of every DB-migrate, seed and stack-recreate stage,
// so a red suite makes the ship stages unreachable (CiPipelineTruthGateTests /
// DeployRedPathProofTests parse this file to enforce that).
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file: never let a failing suite
// resolve to a green build result, and never enumerate suites instead of executing them.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          bash deploy/scripts/ensure-runtime-env.sh "${ENV_FILE}" "${PRESERVE_DIR}" env/profiles/server.env.example
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 1-3: every suite blocking and ordered BEFORE any migrate/seed/deploy,
    // so a red suite makes the ship stages below unreachable. Each suite owns an ephemeral
    // database/stack and does not depend on the production app DB migrated later.
    stage('3. Backend tests - BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          export PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}"
          export ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}"
          export PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_RUNTIME_CONNECTION_STRING:-$PPIQ_TEST_CONNECTION_STRING}"
          export PPIQ_RLS_TEST_CONNECTION_STRING="${PPIQ_TEST_RUNTIME_CONNECTION_STRING:-$PPIQ_TEST_CONNECTION_STRING}"
          export PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_RUNTIME_CONNECTION_STRING:-$PPIQ_TEST_CONNECTION_STRING}"
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          dotnet test Backend --nologo
        '''
      }
    }

    stage('4. Frontend unit tests - BLOCKING') {
      steps { dir("${FRONTEND_DIR}") { sh 'set -euo pipefail; npm ci; npm run test' } }
    }

    stage('5. Frontend e2e (gated off by default; set PPIQ_RUN_E2E=on to enable)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_RUN_E2E:-off}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 4: main app DB migrated (structure) + seeded (latest data) - model-first.
    // Reached only after every suite above is green.
    stage('6. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # 6a. EF migrations FIRST - create the model-derived schema (incl. audit_log_entries)
          #     so the numbered SQL decoration scripts have their dependency tables.
          dotnet ef database update \
            --project "${INFRA_PROJ}" --startup-project "${API_PROJ}" --no-build || \
          dotnet ef database update \
            --project "${INFRA_PROJ}" --startup-project "${API_PROJ}"

          # 6b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 6c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 5: demo DBs migrated + seeded - only when this host runs them.
    stage('7. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    // deploy-canonical.sh performs the health gate and rolls back to the prior image on
    // failure (rollback), so a bad image never stays live.
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults - Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED - deploy/migrate/seed stages did NOT run. Fix the failing suite; nothing was shipped and no rollback was needed.' }
    success { echo "PIPELINE GREEN - build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\jenkins-siblings-20260625-144742\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=184, SizeKB=9.65, SHA256='538E4F47CBC7ACD1B2EADD3947DF366BAB8E756760A588B8EFF7C9E5AEA03BD5', Modified='2026-06-25 14:47:42']

// =====================================================================================
// PlantProcess IQ - canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> e2e (BLOCK)
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> build + recreate canonical stack (health gate + roll back on failure)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// Why tests run FIRST: a deploy must be unreachable while any suite is red. Every test
// stage is textually ordered ahead of every DB-migrate, seed and stack-recreate stage,
// so a red suite makes the ship stages unreachable (CiPipelineTruthGateTests /
// DeployRedPathProofTests parse this file to enforce that).
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file: never let a failing suite
// resolve to a green build result, and never enumerate suites instead of executing them.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          bash deploy/scripts/ensure-runtime-env.sh "${ENV_FILE}" "${PRESERVE_DIR}" env/profiles/server.env.example
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 1-3: every suite blocking and ordered BEFORE any migrate/seed/deploy,
    // so a red suite makes the ship stages below unreachable. Each suite owns an ephemeral
    // database/stack and does not depend on the production app DB migrated later.
    stage('3. Backend tests - BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          SELF="$(cat /etc/hostname)"
          SDK_IMAGE="${PPIQ_SDK_IMAGE:-mcr.microsoft.com/dotnet/sdk:9.0}"
          TESTNET="${PPIQ_CI_TESTDB_NETWORK:-ppiq-citestnet}"
          # the agent has no dotnet; run the suite inside the SDK image, on the test-db network,
          # with the workspace inherited via --volumes-from (same paths as the Jenkins container).
          docker run --rm --network "${TESTNET}" --volumes-from "${SELF}" -w "${PWD}" \
            -e PPIQ_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_RLS_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            "${SDK_IMAGE}" bash -lc 'dotnet test Backend --nologo'
        '''
      }
    }

    stage('4. Frontend unit tests - BLOCKING') {
      steps { dir("${FRONTEND_DIR}") { sh 'set -euo pipefail; npm ci; npm run test' } }
    }

    stage('5. Frontend e2e (gated off by default; set PPIQ_RUN_E2E=on to enable)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_RUN_E2E:-off}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 4: main app DB migrated (structure) + seeded (latest data) - model-first.
    // Reached only after every suite above is green.
    stage('6. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # 6a. Bring up ONLY the app DB first so migrate-and-seed can docker exec into it.
          #     (model-first schema is generated offline by 'ef migrations script --idempotent').
          docker compose -p "${COMPOSE_PROJECT}" --env-file "${ENV_FILE}" -f "${COMPOSE_BASE}" -f "${COMPOSE_SERVER}" up -d plantprocess-postgres
          for i in $(seq 1 30); do
            docker exec plantprocess-postgres pg_isready -U "${POSTGRES_USER}" -d postgres >/dev/null 2>&1 && break
            sleep 2
            [ "$i" = "30" ] && { echo "FATAL: app postgres never became ready"; exit 1; }
          done

          # 6b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 6c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 5: demo DBs migrated + seeded - only when this host runs them.
    stage('7. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    // deploy-canonical.sh performs the health gate and rolls back to the prior image on
    // failure (rollback), so a bad image never stays live.
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults - Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED - deploy/migrate/seed stages did NOT run. Fix the failing suite; nothing was shipped and no rollback was needed.' }
    success { echo "PIPELINE GREEN - build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\line101-20260625-153248\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=193, SizeKB=10.05, SHA256='431C04A767C0BCC841228EAE26AC89D974036EF5662AEE8EE2AE7D5512F0949C', Modified='2026-06-25 15:27:42']

// =====================================================================================
// PlantProcess IQ - canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> e2e (BLOCK)
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> build + recreate canonical stack (health gate + roll back on failure)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// Why tests run FIRST: a deploy must be unreachable while any suite is red. Every test
// stage is textually ordered ahead of every DB-migrate, seed and stack-recreate stage,
// so a red suite makes the ship stages unreachable (CiPipelineTruthGateTests /
// DeployRedPathProofTests parse this file to enforce that).
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file: never let a failing suite
// resolve to a green build result, and never enumerate suites instead of executing them.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          bash deploy/scripts/ensure-runtime-env.sh "${ENV_FILE}" "${PRESERVE_DIR}" env/profiles/server.env.example
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 1-3: every suite blocking and ordered BEFORE any migrate/seed/deploy,
    // so a red suite makes the ship stages below unreachable. Each suite owns an ephemeral
    // database/stack and does not depend on the production app DB migrated later.
    stage('3. Backend tests - BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          SELF="$(cat /etc/hostname)"
          SDK_IMAGE="${PPIQ_SDK_IMAGE:-mcr.microsoft.com/dotnet/sdk:9.0}"
          TESTNET="${PPIQ_CI_TESTDB_NETWORK:-ppiq-citestnet}"
          # the agent has no dotnet; run the suite inside the SDK image, on the test-db network,
          # with the workspace inherited via --volumes-from (same paths as the Jenkins container).
          docker run --rm --network "${TESTNET}" --volumes-from "${SELF}" -w "${PWD}" \
            -e PPIQ_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_RLS_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            "${SDK_IMAGE}" bash -lc 'dotnet test Backend --nologo'
        '''
      }
    }

    stage('4. Frontend unit tests - BLOCKING') {
      steps {
        sh '''
          set -euo pipefail
          SELF="$(cat /etc/hostname)"
          NODE_IMAGE="${PPIQ_NODE_IMAGE:-node:24-alpine}"
          # Run npm INSIDE the node container. The whole command is ONE double-quoted bash -lc
          # argument (heredocs and single-quoted args get mangled inside Jenkins sh ''' blocks).
          docker run --rm --volumes-from "${SELF}" -w "${PWD}/${FRONTEND_DIR}" "${NODE_IMAGE}" sh -lc "set -e; npm ci; npm run test"
        '''
      }
    }

    stage('5. Frontend e2e (gated off by default; set PPIQ_RUN_E2E=on to enable)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_RUN_E2E:-off}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 4: main app DB migrated (structure) + seeded (latest data) - model-first.
    // Reached only after every suite above is green.
    stage('6. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # 6a. Bring up ONLY the app DB first so migrate-and-seed can docker exec into it.
          #     (model-first schema is generated offline by 'ef migrations script --idempotent').
          docker compose -p "${COMPOSE_PROJECT}" --env-file "${ENV_FILE}" -f "${COMPOSE_BASE}" -f "${COMPOSE_SERVER}" up -d plantprocess-postgres
          for i in $(seq 1 30); do
            docker exec plantprocess-postgres pg_isready -U "${POSTGRES_USER}" -d postgres >/dev/null 2>&1 && break
            sleep 2
            [ "$i" = "30" ] && { echo "FATAL: app postgres never became ready"; exit 1; }
          done

          # 6b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 6c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 5: demo DBs migrated + seeded - only when this host runs them.
    stage('7. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    // deploy-canonical.sh performs the health gate and rolls back to the prior image on
    // failure (rollback), so a bad image never stays live.
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults - Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED - deploy/migrate/seed stages did NOT run. Fix the failing suite; nothing was shipped and no rollback was needed.' }
    success { echo "PIPELINE GREEN - build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\reproducible-env-20260622-142358\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=163, SizeKB=7.9, SHA256='D3D4EF32A02991A9F82969A490755FA77A5274AC59E6CA52610766C86CD355F3', Modified='2026-06-17 11:12:08']

// =====================================================================================
// PlantProcess IQ — canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> npm run e2e (BLOCK)
//   -> build + recreate canonical stack (health gate + rollback)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file:
//   do NOT add catchError(buildResult:'SUCCESS') and do NOT add --list to any suite.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          test -f "${ENV_FILE}" || { echo "FATAL: ${ENV_FILE} missing on host. Seed it from env/profiles/*.example."; exit 1; }
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 1: main app DB migrated (structure) + seeded (latest data) — model-first.
    stage('3. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # 3a. EF migrations FIRST — create the model-derived schema (incl. audit_log_entries)
          #     so the numbered SQL decoration scripts have their dependency tables.
          dotnet ef database update \
            --project "${INFRA_PROJ}" --startup-project "${API_PROJ}" --no-build || \
          dotnet ef database update \
            --project "${INFRA_PROJ}" --startup-project "${API_PROJ}"

          # 3b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 3c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 2: demo DBs migrated + seeded — only when this host runs them.
    stage('4. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 3-5: every suite blocking; a red suite stops the deploy.
    stage('5. Backend tests — BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          export PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}"
          export ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}"
          export PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}"
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          dotnet test Backend --nologo
        '''
      }
    }

    stage('6. Frontend unit tests — BLOCKING') {
      steps { dir("${FRONTEND_DIR}") { sh 'set -euo pipefail; npm ci; npm run test' } }
    }

    stage('7. Frontend e2e — BLOCKING (ephemeral CI stack)') {
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults — Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED — deploy stages did NOT run. Fix the failing suite; nothing was shipped.' }
    success { echo "PIPELINE GREEN — build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\reproducible-env-20260622-143803\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=159, SizeKB=7.76, SHA256='96959EC961976D90E0FCE0573876DFCBA332857491459250307CCE46DA938A29', Modified='2026-06-22 13:24:39']

// =====================================================================================
// PlantProcess IQ — canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> npm run e2e (BLOCK)
//   -> build + recreate canonical stack (health gate + rollback)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file:
//   do NOT add catchError(buildResult:'SUCCESS') and do NOT add --list to any suite.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          test -f "${ENV_FILE}" || { echo "FATAL: ${ENV_FILE} missing on host. Seed it from env/profiles/*.example."; exit 1; }
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 1: main app DB migrated (structure) + seeded (latest data) — model-first.
    stage('3. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # EF-first is owned by the canonical migrate path (migrate-and-seed.sh --app-only):
          # it generates an idempotent script FROM the EF model and applies it BEFORE the
          # numbered SQL. The duplicated inline 'dotnet ef database update' was removed (V1-01).
          # 3b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 3c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 2: demo DBs migrated + seeded — only when this host runs them.
    stage('4. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 3-5: every suite blocking; a red suite stops the deploy.
    stage('5. Backend tests — BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          export PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}"
          export ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}"
          export PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}"
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          dotnet test Backend --nologo
        '''
      }
    }

    stage('6. Frontend unit tests — BLOCKING') {
      steps { dir("${FRONTEND_DIR}") { sh 'set -euo pipefail; npm ci; npm run test' } }
    }

    stage('7. Frontend e2e — BLOCKING (ephemeral CI stack)') {
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults — Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED — deploy stages did NOT run. Fix the failing suite; nothing was shipped.' }
    success { echo "PIPELINE GREEN — build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\reproducible-env-20260622-144139\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=159, SizeKB=7.76, SHA256='96959EC961976D90E0FCE0573876DFCBA332857491459250307CCE46DA938A29', Modified='2026-06-22 13:24:39']

// =====================================================================================
// PlantProcess IQ — canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> npm run e2e (BLOCK)
//   -> build + recreate canonical stack (health gate + rollback)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file:
//   do NOT add catchError(buildResult:'SUCCESS') and do NOT add --list to any suite.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          test -f "${ENV_FILE}" || { echo "FATAL: ${ENV_FILE} missing on host. Seed it from env/profiles/*.example."; exit 1; }
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 1: main app DB migrated (structure) + seeded (latest data) — model-first.
    stage('3. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # EF-first is owned by the canonical migrate path (migrate-and-seed.sh --app-only):
          # it generates an idempotent script FROM the EF model and applies it BEFORE the
          # numbered SQL. The duplicated inline 'dotnet ef database update' was removed (V1-01).
          # 3b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 3c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 2: demo DBs migrated + seeded — only when this host runs them.
    stage('4. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 3-5: every suite blocking; a red suite stops the deploy.
    stage('5. Backend tests — BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          export PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}"
          export ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}"
          export PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}"
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          dotnet test Backend --nologo
        '''
      }
    }

    stage('6. Frontend unit tests — BLOCKING') {
      steps { dir("${FRONTEND_DIR}") { sh 'set -euo pipefail; npm ci; npm run test' } }
    }

    stage('7. Frontend e2e — BLOCKING (ephemeral CI stack)') {
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults — Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED — deploy stages did NOT run. Fix the failing suite; nothing was shipped.' }
    success { echo "PIPELINE GREEN — build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\reproducible-env-20260622-145837\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=159, SizeKB=7.76, SHA256='96959EC961976D90E0FCE0573876DFCBA332857491459250307CCE46DA938A29', Modified='2026-06-22 13:24:39']

// =====================================================================================
// PlantProcess IQ — canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> npm run e2e (BLOCK)
//   -> build + recreate canonical stack (health gate + rollback)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file:
//   do NOT add catchError(buildResult:'SUCCESS') and do NOT add --list to any suite.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          test -f "${ENV_FILE}" || { echo "FATAL: ${ENV_FILE} missing on host. Seed it from env/profiles/*.example."; exit 1; }
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 1: main app DB migrated (structure) + seeded (latest data) — model-first.
    stage('3. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # EF-first is owned by the canonical migrate path (migrate-and-seed.sh --app-only):
          # it generates an idempotent script FROM the EF model and applies it BEFORE the
          # numbered SQL. The duplicated inline 'dotnet ef database update' was removed (V1-01).
          # 3b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 3c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 2: demo DBs migrated + seeded — only when this host runs them.
    stage('4. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 3-5: every suite blocking; a red suite stops the deploy.
    stage('5. Backend tests — BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          export PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}"
          export ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}"
          export PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}"
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          dotnet test Backend --nologo
        '''
      }
    }

    stage('6. Frontend unit tests — BLOCKING') {
      steps { dir("${FRONTEND_DIR}") { sh 'set -euo pipefail; npm ci; npm run test' } }
    }

    stage('7. Frontend e2e — BLOCKING (ephemeral CI stack)') {
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults — Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED — deploy stages did NOT run. Fix the failing suite; nothing was shipped.' }
    success { echo "PIPELINE GREEN — build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\stage4-heredoc-fix-20260625-152547\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=198, SizeKB=10.13, SHA256='48AD860A6B85955B4825354EE63BAD5476AADFA239931948D289BDAAF8482A90', Modified='2026-06-25 15:15:30']

// =====================================================================================
// PlantProcess IQ - canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> e2e (BLOCK)
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> build + recreate canonical stack (health gate + roll back on failure)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// Why tests run FIRST: a deploy must be unreachable while any suite is red. Every test
// stage is textually ordered ahead of every DB-migrate, seed and stack-recreate stage,
// so a red suite makes the ship stages unreachable (CiPipelineTruthGateTests /
// DeployRedPathProofTests parse this file to enforce that).
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file: never let a failing suite
// resolve to a green build result, and never enumerate suites instead of executing them.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          bash deploy/scripts/ensure-runtime-env.sh "${ENV_FILE}" "${PRESERVE_DIR}" env/profiles/server.env.example
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 1-3: every suite blocking and ordered BEFORE any migrate/seed/deploy,
    // so a red suite makes the ship stages below unreachable. Each suite owns an ephemeral
    // database/stack and does not depend on the production app DB migrated later.
    stage('3. Backend tests - BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          SELF="$(cat /etc/hostname)"
          SDK_IMAGE="${PPIQ_SDK_IMAGE:-mcr.microsoft.com/dotnet/sdk:9.0}"
          TESTNET="${PPIQ_CI_TESTDB_NETWORK:-ppiq-citestnet}"
          # the agent has no dotnet; run the suite inside the SDK image, on the test-db network,
          # with the workspace inherited via --volumes-from (same paths as the Jenkins container).
          docker run --rm --network "${TESTNET}" --volumes-from "${SELF}" -w "${PWD}" \
            -e PPIQ_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_RLS_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            "${SDK_IMAGE}" bash -lc 'dotnet test Backend --nologo'
        '''
      }
    }

    stage('4. Frontend unit tests - BLOCKING') {
      steps {
        sh '''
          set -euo pipefail
          SELF="$(cat /etc/hostname)"
          NODE_IMAGE="${PPIQ_NODE_IMAGE:-node:24-alpine}"
          # Pass the whole npm command via a heredoc to `sh -s`, so the `&&` runs INSIDE the
          # container (a bare `sh -lc '...'` here had its quotes stripped by Jenkins, leaking
          # `&& npm run test` onto the dotnet-less agent -> exit 127).
          docker run --rm --volumes-from "${SELF}" -w "${PWD}/${FRONTEND_DIR}" "${NODE_IMAGE}" sh -s <<'NPMEOF'
set -e
npm ci
npm run test
NPMEOF
        '''
      }
    }

    stage('5. Frontend e2e (gated off by default; set PPIQ_RUN_E2E=on to enable)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_RUN_E2E:-off}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 4: main app DB migrated (structure) + seeded (latest data) - model-first.
    // Reached only after every suite above is green.
    stage('6. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # 6a. Bring up ONLY the app DB first so migrate-and-seed can docker exec into it.
          #     (model-first schema is generated offline by 'ef migrations script --idempotent').
          docker compose -p "${COMPOSE_PROJECT}" --env-file "${ENV_FILE}" -f "${COMPOSE_BASE}" -f "${COMPOSE_SERVER}" up -d plantprocess-postgres
          for i in $(seq 1 30); do
            docker exec plantprocess-postgres pg_isready -U "${POSTGRES_USER}" -d postgres >/dev/null 2>&1 && break
            sleep 2
            [ "$i" = "30" ] && { echo "FATAL: app postgres never became ready"; exit 1; }
          done

          # 6b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 6c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 5: demo DBs migrated + seeded - only when this host runs them.
    stage('7. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    // deploy-canonical.sh performs the health gate and rolls back to the prior image on
    // failure (rollback), so a bad image never stays live.
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults - Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED - deploy/migrate/seed stages did NOT run. Fix the failing suite; nothing was shipped and no rollback was needed.' }
    success { echo "PIPELINE GREEN - build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\stage4-quote-20260625-151530\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=191, SizeKB=9.86, SHA256='8DB1131A647F1BFB826835B80F016EBE77E2EDEDC1244C430EEC287FF22A4015', Modified='2026-06-25 14:47:42']

// =====================================================================================
// PlantProcess IQ - canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> e2e (BLOCK)
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> build + recreate canonical stack (health gate + roll back on failure)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// Why tests run FIRST: a deploy must be unreachable while any suite is red. Every test
// stage is textually ordered ahead of every DB-migrate, seed and stack-recreate stage,
// so a red suite makes the ship stages unreachable (CiPipelineTruthGateTests /
// DeployRedPathProofTests parse this file to enforce that).
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file: never let a failing suite
// resolve to a green build result, and never enumerate suites instead of executing them.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          bash deploy/scripts/ensure-runtime-env.sh "${ENV_FILE}" "${PRESERVE_DIR}" env/profiles/server.env.example
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 1-3: every suite blocking and ordered BEFORE any migrate/seed/deploy,
    // so a red suite makes the ship stages below unreachable. Each suite owns an ephemeral
    // database/stack and does not depend on the production app DB migrated later.
    stage('3. Backend tests - BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          SELF="$(cat /etc/hostname)"
          SDK_IMAGE="${PPIQ_SDK_IMAGE:-mcr.microsoft.com/dotnet/sdk:9.0}"
          TESTNET="${PPIQ_CI_TESTDB_NETWORK:-ppiq-citestnet}"
          # the agent has no dotnet; run the suite inside the SDK image, on the test-db network,
          # with the workspace inherited via --volumes-from (same paths as the Jenkins container).
          docker run --rm --network "${TESTNET}" --volumes-from "${SELF}" -w "${PWD}" \
            -e PPIQ_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_RLS_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            "${SDK_IMAGE}" bash -lc 'dotnet test Backend --nologo'
        '''
      }
    }

    stage('4. Frontend unit tests - BLOCKING') {
      steps {
        sh '''
          set -euo pipefail
          SELF="$(cat /etc/hostname)"
          NODE_IMAGE="${PPIQ_NODE_IMAGE:-node:24-alpine}"
          docker run --rm --volumes-from "${SELF}" -w "${PWD}/${FRONTEND_DIR}" "${NODE_IMAGE}" sh -lc 'npm ci && npm run test'
        '''
      }
    }

    stage('5. Frontend e2e (gated off by default; set PPIQ_RUN_E2E=on to enable)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_RUN_E2E:-off}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 4: main app DB migrated (structure) + seeded (latest data) - model-first.
    // Reached only after every suite above is green.
    stage('6. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # 6a. Bring up ONLY the app DB first so migrate-and-seed can docker exec into it.
          #     (model-first schema is generated offline by 'ef migrations script --idempotent').
          docker compose -p "${COMPOSE_PROJECT}" --env-file "${ENV_FILE}" -f "${COMPOSE_BASE}" -f "${COMPOSE_SERVER}" up -d plantprocess-postgres
          for i in $(seq 1 30); do
            docker exec plantprocess-postgres pg_isready -U "${POSTGRES_USER}" -d postgres >/dev/null 2>&1 && break
            sleep 2
            [ "$i" = "30" ] && { echo "FATAL: app postgres never became ready"; exit 1; }
          done

          # 6b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 6c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 5: demo DBs migrated + seeded - only when this host runs them.
    stage('7. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    // deploy-canonical.sh performs the health gate and rolls back to the prior image on
    // failure (rollback), so a bad image never stays live.
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults - Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED - deploy/migrate/seed stages did NOT run. Fix the failing suite; nothing was shipped and no rollback was needed.' }
    success { echo "PIPELINE GREEN - build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\stage4-shfix-20260625-152742\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=193, SizeKB=10.05, SHA256='0370E0B6A2E744FC0EAA690B95016B21EE7021D0CDEE53C53F18352299873C2D', Modified='2026-06-25 15:25:47']

// =====================================================================================
// PlantProcess IQ - canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> e2e (BLOCK)
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> build + recreate canonical stack (health gate + roll back on failure)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// Why tests run FIRST: a deploy must be unreachable while any suite is red. Every test
// stage is textually ordered ahead of every DB-migrate, seed and stack-recreate stage,
// so a red suite makes the ship stages unreachable (CiPipelineTruthGateTests /
// DeployRedPathProofTests parse this file to enforce that).
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file: never let a failing suite
// resolve to a green build result, and never enumerate suites instead of executing them.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'plantprocessiq'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          bash deploy/scripts/ensure-runtime-env.sh "${ENV_FILE}" "${PRESERVE_DIR}" env/profiles/server.env.example
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 1-3: every suite blocking and ordered BEFORE any migrate/seed/deploy,
    // so a red suite makes the ship stages below unreachable. Each suite owns an ephemeral
    // database/stack and does not depend on the production app DB migrated later.
    stage('3. Backend tests - BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          SELF="$(cat /etc/hostname)"
          SDK_IMAGE="${PPIQ_SDK_IMAGE:-mcr.microsoft.com/dotnet/sdk:9.0}"
          TESTNET="${PPIQ_CI_TESTDB_NETWORK:-ppiq-citestnet}"
          # the agent has no dotnet; run the suite inside the SDK image, on the test-db network,
          # with the workspace inherited via --volumes-from (same paths as the Jenkins container).
          docker run --rm --network "${TESTNET}" --volumes-from "${SELF}" -w "${PWD}" \
            -e PPIQ_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_RLS_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            "${SDK_IMAGE}" bash -lc 'dotnet test Backend --nologo'
        '''
      }
    }

    stage('4. Frontend unit tests - BLOCKING') {
      steps {
        sh '''
          set -euo pipefail
          SELF="$(cat /etc/hostname)"
          NODE_IMAGE="${PPIQ_NODE_IMAGE:-node:24-alpine}"
          # Run npm INSIDE the node container. The whole command is ONE double-quoted bash -lc
          # argument (heredocs and single-quoted args get mangled inside Jenkins sh ''' blocks).
          docker run --rm --volumes-from "${SELF}" -w "${PWD}/${FRONTEND_DIR}" "${NODE_IMAGE}" bash -lc "set -e; npm ci; npm run test"
        '''
      }
    }

    stage('5. Frontend e2e (gated off by default; set PPIQ_RUN_E2E=on to enable)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_RUN_E2E:-off}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 4: main app DB migrated (structure) + seeded (latest data) - model-first.
    // Reached only after every suite above is green.
    stage('6. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # 6a. Bring up ONLY the app DB first so migrate-and-seed can docker exec into it.
          #     (model-first schema is generated offline by 'ef migrations script --idempotent').
          docker compose -p "${COMPOSE_PROJECT}" --env-file "${ENV_FILE}" -f "${COMPOSE_BASE}" -f "${COMPOSE_SERVER}" up -d plantprocess-postgres
          for i in $(seq 1 30); do
            docker exec plantprocess-postgres pg_isready -U "${POSTGRES_USER}" -d postgres >/dev/null 2>&1 && break
            sleep 2
            [ "$i" = "30" ] && { echo "FATAL: app postgres never became ready"; exit 1; }
          done

          # 6b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 6c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 5: demo DBs migrated + seeded - only when this host runs them.
    stage('7. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    // deploy-canonical.sh performs the health gate and rolls back to the prior image on
    // failure (rollback), so a bad image never stays live.
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults - Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED - deploy/migrate/seed stages did NOT run. Fix the failing suite; nothing was shipped and no rollback was needed.' }
    success { echo "PIPELINE GREEN - build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\stage9-extract-20260626-094753\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=193, SizeKB=10.03, SHA256='22A1D78056DB16EE8163B61523003279BAD0833FBACA53DFFF1A28A976ED074F', Modified='2026-06-25 15:50:21']

// =====================================================================================
// PlantProcess IQ - canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> e2e (BLOCK)
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> build + recreate canonical stack (health gate + roll back on failure)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// Why tests run FIRST: a deploy must be unreachable while any suite is red. Every test
// stage is textually ordered ahead of every DB-migrate, seed and stack-recreate stage,
// so a red suite makes the ship stages unreachable (CiPipelineTruthGateTests /
// DeployRedPathProofTests parse this file to enforce that).
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file: never let a failing suite
// resolve to a green build result, and never enumerate suites instead of executing them.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'ppiq-app'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          bash deploy/scripts/ensure-runtime-env.sh "${ENV_FILE}" "${PRESERVE_DIR}" env/profiles/server.env.example
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 1-3: every suite blocking and ordered BEFORE any migrate/seed/deploy,
    // so a red suite makes the ship stages below unreachable. Each suite owns an ephemeral
    // database/stack and does not depend on the production app DB migrated later.
    stage('3. Backend tests - BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          SELF="$(cat /etc/hostname)"
          SDK_IMAGE="${PPIQ_SDK_IMAGE:-mcr.microsoft.com/dotnet/sdk:9.0}"
          TESTNET="${PPIQ_CI_TESTDB_NETWORK:-ppiq-citestnet}"
          # the agent has no dotnet; run the suite inside the SDK image, on the test-db network,
          # with the workspace inherited via --volumes-from (same paths as the Jenkins container).
          docker run --rm --network "${TESTNET}" --volumes-from "${SELF}" -w "${PWD}" \
            -e PPIQ_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_RLS_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            "${SDK_IMAGE}" bash -lc 'dotnet test Backend --nologo'
        '''
      }
    }

    stage('4. Frontend unit tests - BLOCKING') {
      steps {
        sh '''
          set -euo pipefail
          SELF="$(cat /etc/hostname)"
          NODE_IMAGE="${PPIQ_NODE_IMAGE:-node:24-alpine}"
          # Run npm INSIDE the node container. The whole command is ONE double-quoted bash -lc
          # argument: npm runs inside the node container as one sh -lc string.
          docker run --rm --volumes-from "${SELF}" -w "${PWD}/${FRONTEND_DIR}" "${NODE_IMAGE}" sh -lc "set -e; npm ci; npm run test"
        '''
      }
    }

    stage('5. Frontend e2e (gated off by default; set PPIQ_RUN_E2E=on to enable)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_RUN_E2E:-off}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 4: main app DB migrated (structure) + seeded (latest data) - model-first.
    // Reached only after every suite above is green.
    stage('6. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # 6a. Bring up ONLY the app DB first so migrate-and-seed can docker exec into it.
          #     (model-first schema is generated offline by 'ef migrations script --idempotent').
          docker compose -p "${COMPOSE_PROJECT}" --env-file "${ENV_FILE}" -f "${COMPOSE_BASE}" -f "${COMPOSE_SERVER}" up -d plantprocess-postgres
          for i in $(seq 1 30); do
            docker exec plantprocess-postgres pg_isready -U "${POSTGRES_USER}" -d postgres >/dev/null 2>&1 && break
            sleep 2
            [ "$i" = "30" ] && { echo "FATAL: app postgres never became ready"; exit 1; }
          done

          # 6b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 6c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 5: demo DBs migrated + seeded - only when this host runs them.
    stage('7. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    // deploy-canonical.sh performs the health gate and rolls back to the prior image on
    // failure (rollback), so a bad image never stays live.
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults - Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          BASE="${PPIQ_SMOKE_BASE_URL:-http://127.0.0.1:5063}"
          TOKEN_FILE="${PPIQ_PRESENTATION_TOKEN:-deploy/fixtures/license/enterprise.token}"

          # admin login -> bearer (smoke that auth + DB + signing key are all wired)
          BEARER="$(curl -fsS -X POST "$BASE/auth/login" -H 'Content-Type: application/json' \
            -d "{\\"userName\\":\\"${PPIQ_SMOKE_USERNAME:-admin}\\",\\"password\\":\\"${PPIQ_SMOKE_PASSWORD:?set PPIQ_SMOKE_PASSWORD in .env}\\"}" \
            | sed -n 's/.*\\"token\\":\\"\\([^\\"]*\\)\\".*/\\1/p')"
          test -n "$BEARER" || { echo "FATAL: admin login returned no token"; exit 1; }

          # activate the Enterprise signed token for the demo tenant (V5 Ed25519)
          JWS="$(cat "$TOKEN_FILE")"
          curl -fsS -X POST "$BASE/api/v5/licensing/ed25519/activate" \
            -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' \
            -d "{\\"token\\":\\"$JWS\\"}" > /dev/null
          curl -fsS "$BASE/api/v5/licensing/ed25519/current" -H "Authorization: Bearer $BEARER"
          echo "Presentation ready: admin + Enterprise active at $BASE"
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED - deploy/migrate/seed stages did NOT run. Fix the failing suite; nothing was shipped and no rollback was needed.' }
    success { echo "PIPELINE GREEN - build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\stage9-user-20260626-095520\Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=184, SizeKB=9.52, SHA256='DBADD39695BBA23635C69FBFD98294EB77DA2BAFADC97D8CC703512FED352FA4', Modified='2026-06-26 09:47:53']

// =====================================================================================
// PlantProcess IQ - canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> e2e (BLOCK)
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> build + recreate canonical stack (health gate + roll back on failure)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// Why tests run FIRST: a deploy must be unreachable while any suite is red. Every test
// stage is textually ordered ahead of every DB-migrate, seed and stack-recreate stage,
// so a red suite makes the ship stages unreachable (CiPipelineTruthGateTests /
// DeployRedPathProofTests parse this file to enforce that).
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file: never let a failing suite
// resolve to a green build result, and never enumerate suites instead of executing them.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'ppiq-app'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          bash deploy/scripts/ensure-runtime-env.sh "${ENV_FILE}" "${PRESERVE_DIR}" env/profiles/server.env.example
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 1-3: every suite blocking and ordered BEFORE any migrate/seed/deploy,
    // so a red suite makes the ship stages below unreachable. Each suite owns an ephemeral
    // database/stack and does not depend on the production app DB migrated later.
    stage('3. Backend tests - BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          SELF="$(cat /etc/hostname)"
          SDK_IMAGE="${PPIQ_SDK_IMAGE:-mcr.microsoft.com/dotnet/sdk:9.0}"
          TESTNET="${PPIQ_CI_TESTDB_NETWORK:-ppiq-citestnet}"
          # the agent has no dotnet; run the suite inside the SDK image, on the test-db network,
          # with the workspace inherited via --volumes-from (same paths as the Jenkins container).
          docker run --rm --network "${TESTNET}" --volumes-from "${SELF}" -w "${PWD}" \
            -e PPIQ_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_RLS_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            "${SDK_IMAGE}" bash -lc 'dotnet test Backend --nologo'
        '''
      }
    }

    stage('4. Frontend unit tests - BLOCKING') {
      steps {
        sh '''
          set -euo pipefail
          SELF="$(cat /etc/hostname)"
          NODE_IMAGE="${PPIQ_NODE_IMAGE:-node:24-alpine}"
          # Run npm INSIDE the node container. The whole command is ONE double-quoted bash -lc
          # argument: npm runs inside the node container as one sh -lc string.
          docker run --rm --volumes-from "${SELF}" -w "${PWD}/${FRONTEND_DIR}" "${NODE_IMAGE}" sh -lc "set -e; npm ci; npm run test"
        '''
      }
    }

    stage('5. Frontend e2e (gated off by default; set PPIQ_RUN_E2E=on to enable)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_RUN_E2E:-off}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 4: main app DB migrated (structure) + seeded (latest data) - model-first.
    // Reached only after every suite above is green.
    stage('6. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # 6a. Bring up ONLY the app DB first so migrate-and-seed can docker exec into it.
          #     (model-first schema is generated offline by 'ef migrations script --idempotent').
          docker compose -p "${COMPOSE_PROJECT}" --env-file "${ENV_FILE}" -f "${COMPOSE_BASE}" -f "${COMPOSE_SERVER}" up -d plantprocess-postgres
          for i in $(seq 1 30); do
            docker exec plantprocess-postgres pg_isready -U "${POSTGRES_USER}" -d postgres >/dev/null 2>&1 && break
            sleep 2
            [ "$i" = "30" ] && { echo "FATAL: app postgres never became ready"; exit 1; }
          done

          # 6b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 6c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 5: demo DBs migrated + seeded - only when this host runs them.
    stage('7. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    // deploy-canonical.sh performs the health gate and rolls back to the prior image on
    // failure (rollback), so a bad image never stays live.
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults - Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
            steps {
        sh '''
          set -euo pipefail
          # Run the presentation smoke inside a curl sibling on the app network so it can
          # reach the API by container name (the agent cannot hit 127.0.0.1:5063). Workspace
          # mounted via --volumes-from gives the script access to .env and the token fixture.
          SELF="$(cat /etc/hostname)"
          SMOKE_NET="${PPIQ_SMOKE_NETWORK:-${COMPOSE_PROJECT}_plantprocess-private}"
          SMOKE_IMAGE="${PPIQ_SMOKE_CURL_IMAGE:-curlimages/curl:8.10.1}"
          docker run --rm --network "${SMOKE_NET}" --volumes-from "${SELF}" -w "${PWD}" \
            -e ENV_FILE="${ENV_FILE}" "${SMOKE_IMAGE}" sh ./deploy/scripts/presentation-smoke.sh
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED - deploy/migrate/seed stages did NOT run. Fix the failing suite; nothing was shipped and no rollback was needed.' }
    success { echo "PIPELINE GREEN - build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\v1-04-caddydefaults-20260628-163957\Caddyfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Caddyfile', Lines=70, SizeKB=2.21, SHA256='04093A5872DC9A430154D992BBF2F934C33951ECF5AC4088704D88FC9D107938', Modified='2026-06-13 09:14:58']


# PPIQ_REALIZATION_T016_CANONICAL_CADDYFILE
# PPIQ_REALIZATION_T016_CANONICAL_ENV_DEPLOY_V2
# Single canonical PlantProcess IQ Caddyfile.
#
# Environment variables:
# - SITE_HOST: primary application host, e.g. plantprocessiq.example.com or localhost
# - WEBSITE_HOST: optional public website host, e.g. www.plantprocessiq.example.com
# - ACME_EMAIL: certificate contact email
# - CADDY_AUTO_HTTPS: usually "on" or "off" for local config parsing
# - PPIQ_API_UPSTREAM: default ppiq-app-api:5063
# - PPIQ_APP_UPSTREAM: default ppiq-app-web:80
# - PPIQ_WEBSITE_UPSTREAM: default ppiq-website-web:80

{
    email {$ACME_EMAIL:admin@example.invalid}
    auto_https {$CADDY_AUTO_HTTPS:off}
}

{$SITE_HOST:localhost} {
    encode zstd gzip

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        Content-Security-Policy "default-src 'self'; connect-src 'self' https: http://localhost:* ws://localhost:*; img-src 'self' data: blob:; style-src 'self' 'unsafe-inline'; script-src 'self'; font-src 'self' data:; frame-ancestors 'none'; base-uri 'self'; form-action 'self'"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        Referrer-Policy "strict-origin-when-cross-origin"
        Permissions-Policy "camera=(), microphone=(), geolocation=()"
    }

    handle_path /api/* {
        reverse_proxy {$PPIQ_API_UPSTREAM:ppiq-app-api:5063}
    }

    handle_path /health* {
        reverse_proxy {$PPIQ_API_UPSTREAM:ppiq-app-api:5063}
    }

    handle_path /readiness* {
        reverse_proxy {$PPIQ_API_UPSTREAM:ppiq-app-api:5063}
    }

    handle_path /website/* {
        reverse_proxy {$PPIQ_WEBSITE_UPSTREAM:ppiq-website-web:80}
    }

    handle {
        reverse_proxy {$PPIQ_APP_UPSTREAM:ppiq-app-web:80}
    }
}

{$WEBSITE_HOST:website.localhost} {
    encode zstd gzip

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        Referrer-Policy "strict-origin-when-cross-origin"
    }

    handle_path /api/* {
        reverse_proxy {$PPIQ_API_UPSTREAM:ppiq-app-api:5063}
    }

    handle {
        reverse_proxy {$PPIQ_WEBSITE_UPSTREAM:ppiq-website-web:80}
    }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\.ppiq-backups\web-dockerfile-smoke2-20260626-131101\Dockerfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Dockerfile', Lines=24, SizeKB=0.53, SHA256='7A5391A2D45E1A57398B39F6AE07C1BF4AF364F80AB747F923346BB1A0E3E0AF', Modified='2026-05-19 13:09:35']

# ============================================================
# PlantProcess IQ - App Frontend Dockerfile
# React + Vite build served by Nginx
# ============================================================

FROM node:24-alpine AS build
WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

ARG VITE_API_BASE_URL
ENV VITE_API_BASE_URL=$VITE_API_BASE_URL

RUN npm run build

FROM nginx:1.27-alpine AS runtime

COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/dist /usr/share/nginx/html

EXPOSE 80

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\caddy\Caddyfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Caddyfile', Lines=70, SizeKB=2.23, SHA256='EBEBA7F79F8C03EB5A03917FDA89E80DD7E3168E5F0006728C20FA3F9D747978', Modified='2026-06-28 16:39:57']


# PPIQ_REALIZATION_T016_CANONICAL_CADDYFILE
# PPIQ_REALIZATION_T016_CANONICAL_ENV_DEPLOY_V2
# Single canonical PlantProcess IQ Caddyfile.
#
# Environment variables:
# - SITE_HOST: primary application host, e.g. plantprocessiq.example.com or localhost
# - WEBSITE_HOST: optional public website host, e.g. www.plantprocessiq.example.com
# - ACME_EMAIL: certificate contact email
# - CADDY_AUTO_HTTPS: usually "on" or "off" for local config parsing
# - PPIQ_API_UPSTREAM: default plantprocess-api:5063
# - PPIQ_APP_UPSTREAM: default plantprocess-web:80
# - PPIQ_WEBSITE_UPSTREAM: default plantprocess-web:80

{
    email {$ACME_EMAIL:admin@example.invalid}
    auto_https {$CADDY_AUTO_HTTPS:off}
}

{$SITE_HOST:localhost} {
    encode zstd gzip

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        Content-Security-Policy "default-src 'self'; connect-src 'self' https: http://localhost:* ws://localhost:*; img-src 'self' data: blob:; style-src 'self' 'unsafe-inline'; script-src 'self'; font-src 'self' data:; frame-ancestors 'none'; base-uri 'self'; form-action 'self'"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        Referrer-Policy "strict-origin-when-cross-origin"
        Permissions-Policy "camera=(), microphone=(), geolocation=()"
    }

    handle_path /api/* {
        reverse_proxy {$PPIQ_API_UPSTREAM:plantprocess-api:5063}
    }

    handle_path /health* {
        reverse_proxy {$PPIQ_API_UPSTREAM:plantprocess-api:5063}
    }

    handle_path /readiness* {
        reverse_proxy {$PPIQ_API_UPSTREAM:plantprocess-api:5063}
    }

    handle_path /website/* {
        reverse_proxy {$PPIQ_WEBSITE_UPSTREAM:plantprocess-web:80}
    }

    handle {
        reverse_proxy {$PPIQ_APP_UPSTREAM:plantprocess-web:80}
    }
}

{$WEBSITE_HOST:website.localhost} {
    encode zstd gzip

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        Referrer-Policy "strict-origin-when-cross-origin"
    }

    handle_path /api/* {
        reverse_proxy {$PPIQ_API_UPSTREAM:plantprocess-api:5063}
    }

    handle {
        reverse_proxy {$PPIQ_WEBSITE_UPSTREAM:plantprocess-web:80}
    }
}

[FILE_END]

[FILE_START]
[METADATA: Path='deploy\edge-agent\Dockerfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Dockerfile', Lines=12, SizeKB=0.39, SHA256='5E7439BAEE25DC17554CC0E9EFB1D513B4D694BAFF01C531E0F6B2DD51172E98', Modified='2026-06-06 16:00:49']

FROM node:22-alpine

WORKDIR /app

COPY tools/edge-agent/ppiq-edge-agent.cjs /app/ppiq-edge-agent.cjs
COPY tools/edge-agent/edge-agent.sample.json /app/edge-agent.sample.json

RUN mkdir -p /app/spool && chmod +x /app/ppiq-edge-agent.cjs

ENV PPIQ_EDGE_CONFIG=/app/edge-agent.sample.json

CMD ["node", "/app/ppiq-edge-agent.cjs", "--config=/app/edge-agent.sample.json", "--dry-run", "--once"]

[FILE_END]

[FILE_START]
[METADATA: Path='Jenkinsfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Jenkins Pipeline', Lines=184, SizeKB=9.53, SHA256='F92353D920C5C7DA6E63456E0F96ACC0CD83CE09F1C0C022AE61F2CB98E2C7CA', Modified='2026-06-26 09:55:20']

// =====================================================================================
// PlantProcess IQ - canonical deploy pipeline (server & any customer)
//
// SEQUENCE (model-first, fail-loud, nothing shipped on a red suite):
//   checkout (preserve server secrets) -> sweep stale
//   -> dotnet test (BLOCK) -> npm run test (BLOCK) -> e2e (BLOCK)
//   -> EF migrate app DB -> post-EF SQL -> seed latest dataset
//   -> [server/demo] migrate + seed demo sources
//   -> build + recreate canonical stack (health gate + roll back on failure)
//   -> presentation defaults (activate Enterprise token + smoke admin login)
//
// Why tests run FIRST: a deploy must be unreachable while any suite is red. Every test
// stage is textually ordered ahead of every DB-migrate, seed and stack-recreate stage,
// so a red suite makes the ship stages unreachable (CiPipelineTruthGateTests /
// DeployRedPathProofTests parse this file to enforce that).
//
// GENERIC: every credential, DB target, domain, signing key, bootstrap admin and the
// two PPIQ_*_MODE toggles come from the git-ignored env file. Nothing is hardcoded, so
// this same Jenkinsfile runs unchanged on the SOU server and on every customer host.
//
// Guard test (CiPipelineTruthGateTests / T05) parses this file: never let a failing suite
// resolve to a green build result, and never enumerate suites instead of executing them.
// =====================================================================================
pipeline {
  agent any
  options { timestamps(); disableConcurrentBuilds(); timeout(time: 75, unit: 'MINUTES') }

  environment {
    COMPOSE_PROJECT = 'ppiq-app'
    COMPOSE_BASE    = 'deploy/compose/docker-compose.yml'
    COMPOSE_SERVER  = 'deploy/compose/docker-compose.server.yml'
    COMPOSE_SOURCES = 'deploy/compose/docker-compose.sources.yml'
    ENV_FILE        = 'deploy/compose/.env'          // git-ignored runtime secrets
    FRONTEND_DIR    = 'Frontend/PlantProcess.Web'
    INFRA_PROJ      = 'Backend/PlantProcess.Infrastructure'
    API_PROJ        = 'Backend/PlantProcess.Api'
    PRESERVE_DIR    = '/var/lib/ppiq-preserve'        // keep-area for .env / Caddyfile
  }

  stages {

    // ---------------------------------------------------------------------------------
    stage('1. Checkout (preserve server secrets)') {
      steps {
        sh '''
          set -euo pipefail
          mkdir -p "${PRESERVE_DIR}"
          [ -f "${ENV_FILE}" ] && cp -f "${ENV_FILE}" "${PRESERVE_DIR}/.env" || true
          [ -f deploy/caddy/Caddyfile ] && cp -f deploy/caddy/Caddyfile "${PRESERVE_DIR}/Caddyfile" || true
        '''
        checkout scm
        sh '''
          set -euo pipefail
          mkdir -p "$(dirname "${ENV_FILE}")"
          [ -f "${PRESERVE_DIR}/.env" ] && cp -f "${PRESERVE_DIR}/.env" "${ENV_FILE}" || true
          [ -f "${PRESERVE_DIR}/Caddyfile" ] && cp -f "${PRESERVE_DIR}/Caddyfile" deploy/caddy/Caddyfile || true
          bash deploy/scripts/ensure-runtime-env.sh "${ENV_FILE}" "${PRESERVE_DIR}" env/profiles/server.env.example
        '''
      }
    }

    stage('2. Sweep stale processes & workspace locks') {
      steps { sh 'bash deploy/scripts/sweep-stale.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENTS 1-3: every suite blocking and ordered BEFORE any migrate/seed/deploy,
    // so a red suite makes the ship stages below unreachable. Each suite owns an ephemeral
    // database/stack and does not depend on the production app DB migrated later.
    stage('3. Backend tests - BLOCKING') {
      steps {
        // T05 guard asserts this exact invocation exists and is not wrapped.
        sh '''
          set -euo pipefail
          PPIQ_TEST_CONNECTION_STRING="$(bash deploy/scripts/ci-test-db.sh up)"
          export PPIQ_TEST_CONNECTION_STRING
          trap 'bash deploy/scripts/ci-test-db.sh down' EXIT
          SELF="$(cat /etc/hostname)"
          SDK_IMAGE="${PPIQ_SDK_IMAGE:-mcr.microsoft.com/dotnet/sdk:9.0}"
          TESTNET="${PPIQ_CI_TESTDB_NETWORK:-ppiq-citestnet}"
          # the agent has no dotnet; run the suite inside the SDK image, on the test-db network,
          # with the workspace inherited via --volumes-from (same paths as the Jenkins container).
          docker run --rm --network "${TESTNET}" --volumes-from "${SELF}" -w "${PWD}" \
            -e PPIQ_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_TEST_PG_CONNSTRING="${PPIQ_TEST_CONNECTION_STRING}" \
            -e ConnectionStrings__PlantProcessDb="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_AUDIT_TRIGGER_TEST_CONNECTION="${PPIQ_TEST_CONNECTION_STRING}" \
            -e PPIQ_RLS_TEST_CONNECTION_STRING="${PPIQ_TEST_CONNECTION_STRING}" \
            "${SDK_IMAGE}" bash -lc 'dotnet test Backend --nologo'
        '''
      }
    }

    stage('4. Frontend unit tests - BLOCKING') {
      steps {
        sh '''
          set -euo pipefail
          SELF="$(cat /etc/hostname)"
          NODE_IMAGE="${PPIQ_NODE_IMAGE:-node:24-alpine}"
          # Run npm INSIDE the node container. The whole command is ONE double-quoted bash -lc
          # argument: npm runs inside the node container as one sh -lc string.
          docker run --rm --volumes-from "${SELF}" -w "${PWD}/${FRONTEND_DIR}" "${NODE_IMAGE}" sh -lc "set -e; npm ci; npm run test"
        '''
      }
    }

    stage('5. Frontend e2e (gated off by default; set PPIQ_RUN_E2E=on to enable)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_RUN_E2E:-off}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps { sh 'bash deploy/scripts/ci-e2e-stack.sh' }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 4: main app DB migrated (structure) + seeded (latest data) - model-first.
    // Reached only after every suite above is green.
    stage('6. App DB: EF migrate -> post-EF SQL -> seed') {
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a    # POSTGRES_*, ConnectionStrings__PlantProcessDb, modes

          # 6a. Bring up ONLY the app DB first so migrate-and-seed can docker exec into it.
          #     (model-first schema is generated offline by 'ef migrations script --idempotent').
          docker compose -p "${COMPOSE_PROJECT}" --env-file "${ENV_FILE}" -f "${COMPOSE_BASE}" -f "${COMPOSE_SERVER}" up -d plantprocess-postgres
          for i in $(seq 1 30); do
            docker exec plantprocess-postgres pg_isready -U "${POSTGRES_USER}" -d postgres >/dev/null 2>&1 && break
            sleep 2
            [ "$i" = "30" ] && { echo "FATAL: app postgres never became ready"; exit 1; }
          done

          # 6b. Post-EF SQL decoration (indexes / matviews / ML foundation), idempotent.
          # 6c. Seed the latest committed dataset.
          bash deploy/scripts/migrate-and-seed.sh --app-only
        '''
      }
    }

    // REQUIREMENT 5: demo DBs migrated + seeded - only when this host runs them.
    stage('7. Demo sources: migrate + seed (mode-gated)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_DEMO_SOURCES_MODE:-docker}" != "disabled" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
      steps {
        sh '''
          set -euo pipefail
          set -a; . "${ENV_FILE}"; set +a
          docker compose -p "${COMPOSE_PROJECT}-sources" --env-file "${ENV_FILE}" -f "${COMPOSE_SOURCES}" up -d
          bash deploy/scripts/migrate-and-seed.sh --demo-only   # applies each engine's init/ seeds + deterministic fixtures
        '''
      }
    }

    // ---------------------------------------------------------------------------------
    // REQUIREMENT 6: build + install + run the canonical stack (health gate + rollback).
    // deploy-canonical.sh performs the health gate and rolls back to the prior image on
    // failure (rollback), so a bad image never stays live.
    stage('8. Build + recreate canonical stack') {
      steps { sh 'bash deploy/scripts/deploy-canonical.sh' }
    }

    // REQUIREMENT 7: presentation defaults - Enterprise license + admin so the live URL
    // shows every feature. Idempotent; safe to re-run. Uses the committed dev token on
    // demo hosts; a real customer host points PPIQ_PRESENTATION_TOKEN at their own token
    // (or sets PPIQ_PRESENTATION=off to skip).
    stage('9. Presentation defaults (Enterprise + admin smoke)') {
      when { expression { return sh(script: 'set -a; . "${ENV_FILE}"; set +a; [ "${PPIQ_PRESENTATION:-on}" = "on" ] && echo yes || echo no', returnStdout: true).trim() == 'yes' } }
            steps {
        sh '''
          set -euo pipefail
          # Run the presentation smoke inside a curl sibling on the app network so it can
          # reach the API by container name (the agent cannot hit 127.0.0.1:5063). Workspace
          # mounted via --volumes-from gives the script access to .env and the token fixture.
          SELF="$(cat /etc/hostname)"
          SMOKE_NET="${PPIQ_SMOKE_NETWORK:-${COMPOSE_PROJECT}_plantprocess-private}"
          SMOKE_IMAGE="${PPIQ_SMOKE_CURL_IMAGE:-curlimages/curl:8.10.1}"
          docker run --rm --user 0:0 --network "${SMOKE_NET}" --volumes-from "${SELF}" -w "${PWD}" \
            -e ENV_FILE="${ENV_FILE}" "${SMOKE_IMAGE}" sh ./deploy/scripts/presentation-smoke.sh
        '''
      }
    }
  }

  post {
    failure { echo 'PIPELINE RED - deploy/migrate/seed stages did NOT run. Fix the failing suite; nothing was shipped and no rollback was needed.' }
    success { echo "PIPELINE GREEN - build ${env.BUILD_URL} deployed ${env.GIT_COMMIT}" }
  }
}

[FILE_END]

[FILE_START]
[METADATA: Path='Website\PlantProcess.Website\Dockerfile', Category='Infrastructure: Deployment, Docker, Caddy, CI/CD (.github, Jenkins)', Classification='Dockerfile', Lines=23, SizeKB=0.67, SHA256='25677CE630BCBF94CABE4DB028D4189BD7A13209DB8537C744B2B88D50BF75B1', Modified='2026-05-19 14:03:14']

FROM node:24-alpine AS build
WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

ARG VITE_WEBSITE_API_BASE_URL
ENV VITE_WEBSITE_API_BASE_URL=$VITE_WEBSITE_API_BASE_URL

RUN npm run build

FROM nginx:1.27-alpine AS runtime

COPY index.html /usr/share/nginx/html/index.html

COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/dist /usr/share/nginx/html

RUN printf 'server { listen 80; server_name _; root /usr/share/nginx/html; index index.html; location / { try_files $uri $uri/ /index.html; } location /health { access_log off; return 200 "plantprocess-website-ok\\n"; add_header Content-Type text/plain; } }' > /etc/nginx/conf.d/default.conf

EXPOSE 80

[FILE_END]


# PlantProcess IQ - Validation-to-Test Backlog

Generated from: S2B_Batch4C_ValidationToTestBacklog_20260604_121109.csv

This backlog converts useful validation scripts into durable tests without deleting the original scripts yet.

## Summary

| Recommended Action | Count | Lines | Highest Priority |
|---|---:|---:|---|
| CONVERT_TO_DATABASE_INTEGRATION_TEST | 2 | 93 | P1 |
| CONVERT_TO_ENGINE_OR_AI_TEST | 2 | 108 | P1 |
| KEEP_AS_OPERATIONAL_VALIDATION | 19 | 1761 | P2 |
| MANUAL_REVIEW | 1 | 10 | P2 |

## Priority Rules

- P0: Security, SQL safety, auth, tenant isolation, CORS, secrets, RLS.
- P1: Database, AI/ML/analytics, value engine, assistant grounding, UI workflow tests.
- P2: Operational validators, repo hygiene, deploy checks, CI wrappers.

## Detailed Backlog

| Priority | Action | Source | Target Layer | Target Location | Reason | Conversion Notes |
|---|---|---|---|---|---|---|
| P1 | CONVERT_TO_DATABASE_INTEGRATION_TEST | tools\validation\Run-FullValidation.ps1 | Backend database/integration test | Backend\tests\PlantProcess.Infrastructure.IntegrationTests | Database structure and migration readiness should be proven by repeatable integration tests. | Create idempotency, function existence, view existence, seed sanity, and readiness checks. |
| P1 | CONVERT_TO_DATABASE_INTEGRATION_TEST | tools\validation\Validate-BeforeRefactor.ps1 | Backend database/integration test | Backend\tests\PlantProcess.Infrastructure.IntegrationTests | Database structure and migration readiness should be proven by repeatable integration tests. | Create idempotency, function existence, view existence, seed sanity, and readiness checks. |
| P1 | CONVERT_TO_ENGINE_OR_AI_TEST | tools\validation\validate-v7-phase04-phase05-acceptance.cjs | Backend unit/integration test | Backend\tests\PlantProcess.Application.UnitTests or PlantProcess.Api.IntegrationTests | Analytics, value, assistant, and grounding behavior must be deterministic and regression-tested. | Create golden tests for result shape, abstention, provenance, citations, value range, and no invented numbers. |
| P1 | CONVERT_TO_ENGINE_OR_AI_TEST | tools\validation\validate-v7-phase04-phase05-completion.cjs | Backend unit/integration test | Backend\tests\PlantProcess.Application.UnitTests or PlantProcess.Api.IntegrationTests | Analytics, value, assistant, and grounding behavior must be deterministic and regression-tested. | Create golden tests for result shape, abstention, provenance, citations, value range, and no invented numbers. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | Backend\tools\validation\Validate-Dimension5-Dimension8.ps1 | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\prove-standard-import-gate.cjs | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\Run-P00A-StructuralGates.ps1 | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\Validate-Dimension2-Dimension6.ps1 | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\Validate-Dimension7-Brand.ps1 | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\validate-phase01-phase02-gates.mjs | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\Validate-Phase01-Phase02-Gates.ps1 | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\validate-phase01-phase02-v5-gates.mjs | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\Validate-Phase01-Phase02-v5-Gates.ps1 | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\validate-phase03-gates.mjs | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\Validate-Phase1-Hardening-Website.ps1 | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\Validate-Phase2-Step1-Step2.ps1 | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\Validate-Phase2-Step3-Step5.ps1 | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\validate-sql-script-hygiene.cjs | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\validate-t208-exposure.cjs | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\validate-v6-phase01-phase02-completion.cjs | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\validate-v7-phase01.cjs | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\validate-v7-phase01-acceptance.cjs | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | KEEP_AS_OPERATIONAL_VALIDATION | tools\validation\validate-v7-phase02-phase03-acceptance.cjs | Operational script / CI guard | scripts\env, scripts\run, scripts\hygiene, deploy\ci | Environment, deploy, repo hygiene, and port checks are better kept as operational validators or CI guards. | Keep as script for now. Later wire into CI pipeline instead of converting to product unit tests. |
| P2 | MANUAL_REVIEW | tools\validation\Validate-Phase03-Gates.ps1 | Manual review | Documentation/testing backlog | Validation script needs manual review before conversion. | Inspect assertions and decide whether this belongs in unit, integration, E2E, CI, or operational validation. |

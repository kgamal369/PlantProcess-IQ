# PPIQ SESSION HANDOVER — 08-Jul-2026 (Sessions A/B/C of M1 execution)
**Purpose:** full state transfer to a new session. Do NOT re-investigate, re-test, or re-derive anything below — it is all verified. Repo C:\Workspace\PlantProcess-IQ. Local: API :5063 (`.\scripts\run\start-api.ps1 -Profile local`), Web :5173 (`start-web.ps1`), DB ppiq_app@5432 ppiq_dev/ppiq_dev_local_only, login e2eadmin/E2EAdmin123! (token field `accessToken`). psql: `(Get-ChildItem 'C:\Program Files\PostgreSQL\*\bin\psql.exe'|Sort FullName -Desc|Select -First 1).FullName`.

## 1. RULES / MANDATES (Karim's standing orders — binding)
- Fix at SOURCE, generically; never per-machine/per-run workarounds; manual-SQL workaround != done (mandates #9/#13).
- 100% generic/industry-agnostic; no dataset-specific hardcoding; V1 Stage-2 IF/ELSIF ladder accepted V1-only, ERADICATE in M2 (#21). No demo code/pages in app (#19/#25). Naming Golden Rule: no phase/task tokens (#12).
- **Memory #26 (THE core concept): THREE no-code surfaces**, each = select→filter→link→prepare→configure→SAVE definition→DRIVES A JOB: (1) Staging/Import prep, (2) Dashboard/Widget config, (3) Analysis-job config. Keep distinct. Stage-2 generic canonical projection = keystone unblocking 2+3.
- Customer complaints from presentation #1 (all pulled into M1): DB-Link under Admin unexpected (Admin = users/roles/license only → IA restructure), no translation (i18n EN/DE+AR-ready), whole UX must be logical/professional. LogPanel needs word-filter + system/job source toggle.
- PS 5.1 only; ASCII/UTF-8-no-BOM; CRLF; no `&&`; backup-before-edit; packs gated on build with auto-revert; RETIRE any pack whose embeds diverged from disk.
- Backlog v17 = governing (2-sheet format; phases = 35-55h releasable). M1 = 22 tasks/152h/3 phases. HONESTY FLAG raised: 152h vs demo date — Karim's call, unresolved.

## 2. PS/ENV QUIRKS (hard-won — do not relearn)
- Uploads often arrive EMPTY. Workaround: `Copy-Item file "$env:USERPROFILE\Desktop\x.txt"` then upload the .txt, or paste inline.
- Never paste lines containing the `PS C:\...>` prompt (PS runs `PS`=Get-Process → parameter noise; harmless, nothing executed).
- After a `& { }` block, press Enter before pasting the next (else `}& {` parser error).
- Inline JSON/psql -c gets quote-mangled → temp-file + `-f`, BOM-less `[IO.File]::WriteAllText(...,UTF8Encoding($false))`. `Out-File -Encoding utf8` writes BOM psql rejects.
- MySQL: use `MYSQL_PWD=` env not `-p` (warning on stderr paints red in PS). `sh -c '... -e "..."'` from PS strips quotes → docker cp a .sql file instead.
- dotnet build FAILS with MSB3027 file-locks if API running → `Get-Process PlantProcess.Api,dotnet | Stop-Process -Force` first (bake into backend packs).
- Docker Desktop autostart: `Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"`, poll `docker ps` until exit 0 (~60-90s). Source containers exist but stopped after reboot → `docker start ppiq-src-meltshop-postgres ppiq-src-parsytec-mysql ppiq-src-downtime-mysql ppiq-src-pkl-mssql ppiq-src-caster-oracle ppiq-src-hsm-oracle`. Compose file: deploy\compose\docker-compose.sources.yml. Oracle data ephemeral on container RECREATE (not restart).
- App.tsx has a mojibake comment line that breaks line-based Get-Content reads mid-file → read via `[IO.File]::ReadAllText` + Substring on `<Routes` anchor. (Charm-pass item.)

## 3. ARCHITECTURE TRUTH (discovered, verified — critical)
- Pipeline: **native `src_*` tables INSIDE ppiq_app** (NOT foreign tables, NO sync from containers) → `ppiq_register_dump_source(sys,schema,table,pk[],watermark,2,30)` validates LOCAL src_* existence → Stage-1 → `dump_store.src_<schema>_<table>` → Stage-2 ladder → canonical. Registry col = `last_index_column` (NOT watermark_column_name). 5 src_* schemas × 2 tables: src_meltshop_pg(heats,lf_treatment), src_caster_oracle_shape(cast_sequence,cast_pieces), src_hsm_oracle_shape(hsm_coils,hsm_pass_measurements), src_inspection_mysql_shape(parsytec_surface_defects,downtime_events), src_pkl_mssql_shape(pickle_orders,qa_lab_results).
- The six Docker containers + connectors (6/6 green, options-json creds via ConnectorCredentialResolver, committed) are a SEPARATE world from the src_* pipeline. Unification via FDW = **M2 keystone (Option 2)**. Chosen for M1: **Option 1-A** — plant data regenerated INTO the existing demo src_* shapes (zero ladder change, zero debt).
- Stage-2 ladder fn `ppiq_run_stage2_canonical_refresh(p_registry_id,p_requested_by,p_max_minutes DEFAULT 1)` in Backend\database\scripts\130_...sql: watermark from `two_stage_processed_watermarks` (consumer 'STAGE2_CANONICAL'); NULL watermark → condition TRUE (all rows); NOT EXISTS guards on material_code+site; site = `ppiq_ensure_phase3_site()` → PPIQ_P3_SITE 3a000000-0000-0000-0000-0000005140e0. Ladder maps: heats→Heat lots, cast_pieces→Slab+ProducedInto edges, hsm_coils→Coil+RolledInto, parsytec→quality_events. material_code = source id VERBATIM (no transform). UNIQUE(site_id,material_code) + UNIQUE(source_system,source_record_id).
- Endpoint `TwoStageImportEndpoints.cs`: RunStage1Async (line ~278) `maxRows??50000, timeoutSeconds??120`; RunStage2Async `maxMinutes??1`; `_all` variants share ONE budget across tables (the starvation defect). ProvisionBaselineAsync hardcodes the 10 demo tables.

## 4. SESSION A RESULT (M1-01/M1-02) — data state NOW
- Plant generated into demo shapes (genA seed 42): **1,797 heats / 18,629 pieces=slabs / 18,629 coils / 119,109 defects**; CRACK signal 4.5x validated pre-load. Loaded via loadA.sql.gz (repo root, committed) + Apply-SessionA-PlantThroughPipeline.ps1. src_ tables confirmed filled.
- **Canonical NOW (verified):** Heat 2,436 (1,797 plant + 639 old) / Slab 18,073 / Coil 17,823; genealogy **ProducedInto 18,073 + RolledInto 17,821**; quality_events 51,271. Edge sample proven (C0000013→SLB0000204). Stale single-row other-industry demo units (AluminumBillet, TireUnit, etc.) still present — purge pending.
- **M1-02 = DONE** (plant-scale canonical + connected chain). ppiq_walk_genealogy returned [] in our test — takes a tenant/site param we passed wrong ('11111111...' and '3a000000...' both returned []); raw edge JOIN proves chain fine; walk-param investigation = small open item.
- **M1-01 = DIAGNOSED, NOT CURED.** Three proven root causes: (a) Stage-1 HTTP endpoint 500s on large reads ("Exception while reading from stream ~30s", ADO command timeout — the SQL fn `ppiq_run_stage1_delta_import(rid,'x',50000,300)` called directly dumped 1,797 heats fine, so defect is endpoint-layer only); (b) Stage-2 `_all` shares one `maxMinutes??1` budget → starvation (direct per-registry call w/ 60min inserted 12,147 in duration_ms=0); (c) registry `last_index_value_text` from old demo data was AHEAD of plant timestamps (heats stuck at 2026-07-05 vs data max 07-02) → delta filter skipped everything; also `two_stage_processed_watermarks` re-advances via COALESCE. **The manual unblock used:** reset both watermark stores (`UPDATE source_table_dump_registry SET last_index_value_text=NULL WHERE...` + `UPDATE two_stage_processed_watermarks SET last_processed_index_value_text=NULL, processed_rows=0 WHERE consumer_code='STAGE2_CANONICAL'`) then call SQL fns directly per registry with big budget. **The committed fix (TOP of next code session): patch TwoStageImportEndpoints.cs — raise Stage-1 CommandTimeout, per-table budgets in _all, sane defaults, + a first-class watermark-reset/full-reimport endpoint + guard test.** Until then HMI Run Stage-1 button 500s.

## 5. SESSION B RESULT (M1-04 done, M1-03 built)
- **M1-04 DONE, commit ace39f39, proven live.** Discovery existed already: all 4 DB connectors implement `ISchemaReader` (DiscoverDatasetsAsync/DiscoverFieldsForDatasetAsync, live information_schema, read-only sessions); factory `IDataSourceConnectorFactory.GetSchemaReader(providerType)`. Pack Apply-M1-04-LiveDiscovery.ps1 added: SourceDiscoveryDtos.cs; service partial ConnectorConfigurationService.Discovery.030 (pure EF+factory — IPlantProcessDbContext has NO Database facade, so raw SQL lives in ENDPOINT via concrete PlantProcessDbContext, mirroring TwoStageImportEndpoints); 3 literal routes on /admin/connectors/connection-profiles/{id}: GET /tables, GET /tables/{schema}/{table}/columns, POST /register {schemaName,tableName,primaryKeyColumns[],watermarkColumn?,selectedColumns?,rowFilter?} → ppiq_register_dump_source + column-subset/rowFilter persisted to registry source_columns_json (Surface-1 prep seed). **Live proof:** Meltshop /tables → 8 real tables; /columns(meltshop_heats) → 20 columns, heat_id PK-candidate, tap_start_utc timestamp-candidate. Followup logged: **Oracle discovery 400 "schema/object/column name required"** → OracleConnector needs default-schema resolution (ppiq_src). Service fields: `_connectorFactory`, `_dbContext`. Result pattern: ApplicationResult<T> + ApplicationError.Validation/NotFound; endpoints: ApplicationProblems.NotFound exists, .Validation does NOT (use Results.BadRequest).
- **M1-03 BUILT + BUILD GREEN, render UNCONFIRMED.** Pack Apply-M1-03-Surface1-ImportPrep.ps1 (final version, applied): pages/SourceImportPrepPage.tsx — 4-step Surface-1 (profile dropdown filtered to postgresql/mysql/sqlserver/oracle → live /tables grid → /columns with PK/watermark auto-defaults → subset+PK+watermark+rowFilter → Register → "bound to import job"); client methods listSourceTables/listSourceColumns/registerSourceTable inserted after testConnectionProfile in productCoreApiClient.runtime.ts (pattern: getJson/postJson<T> from ./productApiHardening); types appended to api/product-core/shared-types.ts; runtime got injected `import type {...} from "./product-core/shared-types"`; page imports types DIRECTLY from shared-types (barrel doesn't re-export); App.tsx lazy import + route `/data-integration/prepare` via withPageBoundary(path,title,<El/>) inside `<Route element={<AppLayout/>}>`. **StandardPageButton (StandardPageCompat) = plain ButtonHTMLAttributes + isDisabled/isLoading — NO variant prop.** V5NoCodeMapperPage = ORPHAN (routed nowhere, only discover-demo calls) → delete in M1-10. **NEXT ACTION: browse /data-integration/prepare (API+web+containers up) and confirm render; then remove .baks + commit M1-03.**
- **BONUS pre-existing bug FIXED:** frontend main was RED — WidgetBuilderWizardContent.helpers.ts declared `parseJson` without export → `export function parseJson` applied, tsc -b clean. Commit standalone if not yet committed (check `git status`).
- Client indirection chain: productApiClient → productCoreApiClient.implementation → .runtime (+ product-core/shared-types + product-core/types). ConnectionProfileRecord fields: id, connectionProfileCode, connectionProfileName, providerType, schemaName.

## 6. SESSION C RECON (M1-05 Surface-3) — ready to build, NOT built
- Engine chain EXISTS end-to-end: `inspection_jobs` table (defect_type, parameter_code, rule_json, source_correlation_run_id, last_run_*, honest_state, ux_ code unique); `ml_correlation_compute_runs` (target_outcome_key, grain, window_days, status, request_json, RLS tenant); `ml_correlation_results_v2` (feature_key, outcome_key, effect_size, q_value, evidence_json, FK compute_run_id, RLS); fn `ppiq_ml_run_learning_job_v1(p_job_code DEFAULT 'ML_PROCESS_VS_DEFECT', p_outcome_family, p_window_days)`.
- Endpoints EXIST: `/api/ml/learning` group — GET /status, GET /jobs, **POST /jobs/{jobCode}/run** (calls `ppiq_ml_run_learning_job_governed_v1(@jobCode,@windowDays,20,false)` returning readiness_status/readiness_reason — the ReadinessGate!), GET /results; QueryRowsAsync raw-ADO pattern via PlantProcessDbContext. `/analytics/phase2` group — **POST /inspection-jobs/save-from-correlation** (full INSERT, request: InspectionJobCode/Name/InspectionType default RuleBasedQualityWatch/SourceCorrelationRunId/ParameterCode/DefectType/SiteId/EquipmentId/RuleJson/ScheduleExpression/IsEnabled/Description/IsSynthetic), **GET /inspection-jobs**, **POST /rule-correlation/run** (RuleCorrelationRequest: requires ParameterCode + DefectType, FromUtc??now-30d, ToUtc??now; filters `event_type ILIKE %defect%`), /ml-lifecycle + /evaluate.
- **Agreed direction: Option A+** — M1-05 = frontend AnalysisJobConfigPage orchestrating existing endpoints: define (DefectType from live quality data + ParameterCode + window) → Save (save-from-correlation) → Run (rule-correlation/run = genuinely defect+window-filtered; and/or governed learning run for engine path) → results + readiness shown honestly. **Engine limitation stated honestly:** governed fn takes jobCode+windowDays only — arbitrary population predicates (grade=S355J2) NOT a run param → stored in rule_json as declared scope; population-filtered engine = logged M2 remainder (belongs with keystone).
- **Still needed before building:** InspectionJobsPage.tsx (upload kept failing — use Desktop-.txt workaround) + record definitions verbatim: `Get-Content ...Phase2InvestigationEndpoints.cs | Select-Object -Skip 522 -First 90`.
- Acceptance test plan on plant data: CRACK_LONG 30d → superheat/peritectic top driver (planted 9.3x); SCRATCH → no significant driver (control).

## 7. TEST RESULTS LEDGER (do NOT re-run)
- Six connectors test-connect: **6/6 green** (PG/MySQLx2/MSSQL/Oracle x2) — profiles DEMO-READY-CP-01..06, options-json creds; ids dddd0000-...-0201..0208 (01=Meltshop PG, 06=Caster Oracle @11521, 04=HSM Oracle @11522, 05=PKL MSSQL sa/Ppiq_Src_Local_Only1, 02=Downtime 13306, 03=Parsytec 13307; Oracle service FREEPDB1).
- M1-04 live: /tables 8 rows, /columns 20 rows (flags correct); Oracle /tables → typed 400 (followup).
- Canonical counts + genealogy: see §4. Correlation deterministic core: 13 findings render, superheat 0.924/q=0.0001 (earlier session, still valid).
- e2e: 397 tests × 4 projects; first Playwright-owned run wrote visual baselines; login-surface spec dead (cold-load lands authenticated); manual servers MUST be stopped for e2e (Playwright owns 5063/5173 with E2E env). Full realignment = M1 triage + M2 remainder tasks in backlog v17.
- tsc -b: clean after parseJson fix.

## 8. BACKLOG v17 STATUS (M1, 22 tasks/152h; file Documentation/docs/Product RoadMap/PPIQ_Product_Backlog_v17.xlsx)
- M1-01 Stage-1 fallback/timeouts: **diagnosed, cure pending** (top priority code task; see §4).
- M1-02 plant→canonical: **DONE** (via Option 1-A data, zero ladder change — better than the task's planned ladder extension; no eradication debt added).
- M1-03 Surface-1 page: **built+green, render check pending**; remainder logged: cross-source visual linking (canvas).
- M1-04 registry/table REST: **DONE** (+Oracle-schema followup).
- M1-05 Surface-3: **recon complete, Option A+ agreed, build pending** (needs 2 pastes).
- M1-06 IA restructure (Data Integration out of Admin): not started — `/data-integration/prepare` route already anticipates the area.
- Assistant chain (mount orphan AssistantChat → wire POST /api/assistant/ask via api/phase8Assistant.ts → provider from ppiq_assistant_provider_configs): not started; routes /phase8/assistant exist; AssistantChat imported by ZERO components (root cause of "can see it, can't open it").
- i18n EN/DE, LogPanel word-filter+source-toggle, e2e realignment, charm/M1-10 (mojibake Host/DB columns, "Some error message:" prefix, delete V5NoCodeMapperPage, gitignore debris: ladder.txt/loadA.sql.gz/sessA-recon.txt/*.20260609_*/~$*.xlsx): all pending.
- Journey docs: PPIQ_Test_User_Journey_Deep_v3.md (three-surfaces framing, [V]/[I]/[GAP] tags) = walk script; Rehearsal protocol committed earlier.

## 9. DEPLOYMENT / SERVER / PIPELINE (stable knowledge)
- Hetzner VPS 178.105.152.180, two-project topology DELIBERATE, never merge: `plantprocessiq` = sacred infra (Jenkins+Caddy), `ppiq-app` = application deploy. Jenkins auto-deploys on git push (CI truth gate + honesty-lint). Caddy routes to container `plantprocess-web` (historic outage was wrong name `plantprocess-app-web`). Spamhaus/abuse item: lead email must go via authenticated relay, SPF+DKIM, PTR, block outbound 25 (server backlog).
- Local git: main @ ace39f39 (M1-04) after b21a2c0e (dataset+connectors). Commits sweep debris — gitignore pass pending. parseJson fix + M1-03 possibly uncommitted — `git status` first thing.

## 10. IMMEDIATE NEXT ACTIONS (in order)
1. `git status` → commit parseJson fix + M1-03 if pending.
2. Confirm M1-03 renders: API+web up, containers up → http://localhost:5173/data-integration/prepare → Meltshop → 8 tables → meltshop_heats → 20 cols → Register. Then .bak cleanup + commit.
3. Build M1-05 pack (Option A+) after receiving InspectionJobsPage.tsx (.txt workaround) + records slice (skip 522, first 90).
4. M1-01 committed cure (TwoStageImportEndpoints patch + watermark-reset endpoint + guard test) — stop API before dotnet build.
5. Then M1-06 IA restructure / assistant chain / i18n per backlog priority. Purge stale non-steel material_units. Oracle discovery default-schema fix.

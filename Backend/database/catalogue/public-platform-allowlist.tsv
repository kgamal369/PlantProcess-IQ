# PPIQ PUBLIC-SCHEMA PLATFORM ALLOWLIST
#
# Product persistence belongs in ppiq_meta, ppiq_plant or ppiq_staging. A public
# object is platform infrastructure only when it is listed here, and being
# listed here is a decision somebody signs.
#
# FORMAT: object_name<TAB>kind<TAB>lifecycle<TAB>owner<TAB>reason
#
schema_migrations	table	platform	PPIQ Platform	Post-EF SQL ledger. Migration machinery, not product data.
__EFMigrationsHistory	table	platform	PPIQ Platform	EF migration ledger. Platform bookkeeping, not product persistence.
#
# Not a general permission: everything else in public must resolve to a measured
# creator and is catalogued as bounded compatibility with a retirement owner.
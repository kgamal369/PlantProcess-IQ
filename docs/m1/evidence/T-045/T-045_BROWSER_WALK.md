# T-045 BROWSER WALK

The one part of T-045 closure a machine cannot perform.
`Invoke-T045-PageSurfaceCheck.ps1` already proves every widget EXECUTES, keeps
its class shape, and carries no forbidden vocabulary. What remains is whether a
plant engineer can READ the result.

Fill the verdict column. A blank is not a pass.

## Before walking

    powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run\start-api.ps1 -Profile presentation
    .\node_modules\.bin\vite --host localhost --port 5173

## Parameter Deep Analysis

| # | What to see | Verdict |
|---|---|---|
| 1 | The four FDT_C widgets show numbers, not empty states. This page was blank until 813. | |
| 2 | `PA_BYP` and `PA_TABLE` ask different questions - volume by parameter, average by grade. | |

## Correlation / Statistical Findings

| # | What to see | Verdict |
|---|---|---|
| 3 | `CF_RATE` reads "Published Statistical Findings" and states that none are currently published. | |
| 4 | No defect rate or defect count appears anywhere under a correlation heading. | |
| 5 | `CF_TOP` shows the five readiness dimensions, so the page explains WHY there is nothing to publish. | |

## Risk Intelligence

| # | What to see | Verdict |
|---|---|---|
| 6 | `RI_KPI` says "Average Risk Score (Scored Population Only)" - the qualifier is visible, not truncated. | |
| 7 | `RI_TREND` shows coverage and provenance, including `SCORING_SOURCE_SYNTHETIC` and `MODEL_NOT_READY`. | |
| 8 | No one-point trend line. No risk-by-equipment widget. | |
| 9 | Nothing on this page calls the numbers a prediction or a model output. | |

## Model Insights

| # | What to see | Verdict |
|---|---|---|
| 10 | Five readiness dimensions, each with its measured value against its threshold. | |
| 11 | Overall reads Blocked, and the blocking dimension is minority-class balance at 2.0 percent against 3.0. | |
| 12 | No prediction curve. No relabelled defect data. | |

## The question this walk exists for

| # | What to see | Verdict |
|---|---|---|
| 13 | The Class-2 tables carry 8 to 11 columns. Do they render readably, or spill and truncate? | |
| 14 | Are any internal identity columns shown to a customer as ordinary business columns? | |

If 13 or 14 fails, that is a real finding of the same class as D6 and it is fixed
before T-045 is called closed.

## Verdict

    Walked by:
    Date:
    Result:
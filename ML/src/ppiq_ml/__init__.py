"""PPIQ Python ML runtime.

Governance and decisions live in the .NET control plane. This package performs
numerical computation only. It never connects to a database, never decides that a
model is production champion, and never treats console output as a result.
"""

__version__ = "0.1.0"
